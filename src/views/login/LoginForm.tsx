import { type LoginData } from '@/api/user'

import { defineComponent } from "vue";


export default defineComponent({
    name: 'loginForm',
    setup() {
        return () => (
            <div>login form</div>
        )
    }
})